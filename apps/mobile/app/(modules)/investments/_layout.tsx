import { Stack } from 'expo-router';

/** Shell del módulo Inversión (PHASE-44.7). Registra el grupo de tabs. */
export default function InvestmentsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
