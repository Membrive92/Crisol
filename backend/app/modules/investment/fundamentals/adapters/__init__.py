"""Adapters de fundamentales (ARCHITECTURE §3).

`concept_map.py` es PURO (datos + validación del propio mapeo) y no importa
nada de red: se puede testear sin `edgartools` instalado y sin tocar la SEC.
"""
